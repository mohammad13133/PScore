import { View, Text } from "react-native";
import React from "react";

const Line = () => {
  return (
    <View
      style={{
        width: 300,
        backgroundColor: "red",
        borderBottomColor: "black",
        borderBottomWidth: 1,
        marginTop: 8,
      }}
    />
  );
};

export default Line;
